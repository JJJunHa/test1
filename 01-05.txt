유효성검사 -> Validation

반환값 있는 함수--->변수에 들어간 값을 가지고 나옴-> 밖에서 쓸수 있게
스테텍-데이터 타입명- 변수명

반환값 없는 함수--->실행된 결과만 가져옴

지역변수(Local variable)
전역변수(Global variable)
팀원이 여러명이면 전역 변수를 전부 공유해야한다!!
전역 변수를 서로 다른것으로 바꿔서 전역 변수를 쓰는 과정중에 충돌이 나서 변수가 바뀐다

특별한 경우가 아니면 전역변수를 자제해야한다.

객체 : 이름을 붙일 수 있는 유무형의 사물(개념)
----> 클래스(보안에 효과적이다.)
사과, 배, 포도, 복숭아, 저그, 프로토스

class Apple
class Peer
class Peach
class Car

클래스(객체)(bean) 이름은 항상 대문자!!

	    Java         		        C++
속성(변수)---field(클래스안에변수) 	      멤버변수
기능(함수)---method                             멤버함수


객체지향 프로그램밍 언어의 특징
-Encapsulation(캡슐화) -> 클래스안에 함수와 변수가 묶여있다.
-Inharianc(상속)
-Poumobhidm(다형성)
-Abstraction(추상화)

클래스라는 거푸집으로 찍어낸 복제품은 인스턴스!!!
객체변수명.필드명 = 0;







